# Nono 完整优化设计计划（2026-07-12 定稿）

## 核心信息架构（对齐浏览器书签树）

```
一级文件夹（大类）  →  搜索框下方 tabs：[全部] [A] [B] [C] [D] …
  └─ 二级文件夹（每类 6~12 个） →  页面玻璃卡片
       └─ 书签  →  卡片内链接
```

## 阶段总览与顺序

| 阶段 | 内容 | 规模 |
|---|---|---|
| P1 | 后台 UI 重构（配色 token、表单可见性、布局人性化） | 大 |
| P2 | 大分类 tabs：全部+大类过滤、子文件夹归组、后台树形管理 | 大 |
| P4 | 书签/文件夹管理体验改造 | 中 |
| P3 | 前台高度自定义 + 主题预设 | 中 |
| P5 | 全站细节与动画打磨 | 小 |

执行顺序：**P1 → P2 → P4 → P3 → P5**（P2 零数据库迁移，复用现有 `folder.parentId`）。

---

## P1 后台 UI 重构

**现状问题**：输入框边框 `#e2e8f0` 几乎不可见；emerald/蓝/teal 三套主色混用不协调；玻璃态叠浅色表面对比度不足。

1. **配色收敛为一套语义 token**（`tokens.css` 的 `.app-workbench` 作用域）：
   - 唯一主色 = 前台同款 emerald（前后台品牌一致）；语义色 danger / warning / success / info 各一；
   - 表面三层次：页面底（#f6f7f9）/ 卡片（#fff）/ 内嵌控件（#f8fafc + 明确边框），层间保证对比。
2. **表单控件重做**：边框加深至 `#cbd5e1` 级别 + focus 时 accent 边框 + 柔和光环；checkbox/radio/range/select 统一 accent-color；label 字号间距统一；必填/错误态样式。
3. **布局与密度**：卡片圆角/间距/标题层级统一；危险操作按钮 + 确认交互统一；表格行 hover/选中态明确；空态/加载态已统一（EmptyState/LoadingOverlay），保持。
4. 暗色模式：**本期不做**，浅色做精（token 化后未来加暗色成本低）。

## P2 大分类（零迁移，复用 parentId）

**前台**：
- tabs = `[全部]` + 一级文件夹列表，保留吸顶、药丸滑块、溢出渐隐；默认选中"全部"；
- 点大类 tab → 卡片网格**只显示该大类的二级文件夹**，带切换过渡；点"全部"→ 显示所有二级文件夹（按大类顺序排列，可加大类小节标题）；
- 一级文件夹不再渲染为卡片；一级文件夹直挂书签时生成"直属"卡片兜底；
- 搜索时跨大类全局匹配，结果按大类分组展示；scrollspy 在"全部"视图下继续联动 tabs。

**后台**：
- 文件夹页改两级树形管理：大类列表 → 展开子文件夹；拖拽调整归属与排序；
- 新建时明确选择"大类 / 子文件夹（属于哪个大类）"；
- 书签 HTML 导入：第一层 → 大类，第二层 → 子文件夹（核实现有导入的拍平逻辑并对齐）。

**数据**：不加表不迁移；UI 引导两层结构，数据层不强制（兼容已有深层数据，>2 层的按其顶层大类归组）。

## P4 书签/文件夹管理体验

1. **快速添加**：书签页顶部"粘贴 URL 即添加"——服务端抓取页面 title/描述自动填充，选文件夹回车即存；
2. **编辑体验**：书签编辑改侧滑抽屉；列表内联改名；
3. **文件夹树形视图**（与 P2 后台合并实施）：显示书签数、层级拖拽；
4. **书签列表**：保留文件夹筛选，增加"全部"与按域名分组视图；批量工具条优化（选中时吸底常驻）；
5. **导入预览**：可直接调整目标大类/文件夹映射。

## P3 前台高度自定义 + 主题预设

在现有外观系统（圆角/透明度/模糊，存 `site.settings`）之上扩展：

1. **五套内置主题**（一键应用 = accent 色 + 背景方案 + 外观参数 + 字色打包）：
   - 暗夜玻璃（现状精修版）/ 极简白 / 霓虹紫蓝 / 暖纸米色 / 高对比无障碍；
2. **自定义项扩展**：主色取色器、背景（纯色/渐变/图片+遮罩浓度）、字体（字库+字号档）、卡片列宽与间距、全局动画开关；
3. **实现**：`site.settings.theme = { id, overrides }`；前台颜色全部走 CSS 变量（P1 铺路）；AppearanceEditor 顶部加主题卡片墙（缩略预览）；用户改过的项作为 overrides 叠在主题上。

## P5 细节与动画

- 动效 token：duration 120/240/340ms + standard/spring 两条曲线，替换全站零散 cubic-bezier；
- 路由切换过渡（淡入+上移 8px）；
- 列表增删 `TransitionGroup` 位移动画（书签/文件夹/大类）；
- 弹窗开合 spring 化；toast 出入场统一；
- 后台表格骨架屏；
- 全部纳入 `prefers-reduced-motion`。

---

## 遗留工程项（此前计划结转）

- Playwright E2E + 截图回归（需本机装浏览器：`npx playwright install`）；
- Lighthouse 基线度量；
- 前后台"卡/加载不出来"问题排查（优先怀疑后端进程/`/api/auth/session` 挂起阻塞路由守卫）——**建议在 P1 开工前先解决**，否则改完看不到效果。

## 验收基准

- 每阶段：`vue-tsc` 零错误 + web/server 测试全绿 + 视觉契约测试同步更新；
- P2 完成时：三层结构在前台/后台/导入三处行为一致；
- P3 完成时：切换任意主题不出现对比度不可读的组合。
