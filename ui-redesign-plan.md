# UI 重构与自定义化改造计划（2026-07-12）

## 总览

| 阶段 | 内容 | 预估工作量 | 依赖 |
|---|---|---|---|
| P1 | 后台 UI 重构（配色体系 + 表单可见性 + 布局人性化） | 大 | 无 |
| P2 | 大分类（Category）：数据模型 + API + 前台展示 + 后台管理 | 大 | 无 |
| P3 | 前台高度自定义：主题预设 + 自定义面板扩展 | 中 | P1 的 token 体系 |
| P4 | 书签/文件夹管理体验改造 | 中 | P1 |
| P5 | 全站细节与动画打磨 | 小 | P1~P4 落定后统一过 |

建议顺序：P1 → P2 → P4 → P3 → P5。P2 涉及数据库迁移，越早定越好。

---

## P1 后台 UI 重构

**现状问题**：浅色工作台上输入框边框是 `#e2e8f0`（几乎看不见）；绿色（emerald）、蓝色（#2563eb）、青色（teal #0f766e）三套主色混用；玻璃态叠加浅色表面导致对比度不足。

**方案**：
1. **统一后台配色为一套语义 token**（写进 `tokens.css` 的 `.app-workbench` 作用域）：
   - 主色一种（建议沿用前台的 emerald，前后台一致）；语义色 danger/warning/success/info 各一。
   - 表面三层：page bg / card / inset（输入框），层与层之间保证对比。
2. **表单控件全面重做**：输入框边框加深（如 `#cbd5e1`）+ 内阴影区分层级 + focus 时 accent 边框加光环；checkbox/radio/range 统一 accent-color；所有 label 与控件间距、字号统一。
3. **信息密度与布局**：卡片间距/圆角/标题层级统一；危险操作（删除）按钮统一样式与确认交互；表格行 hover/选中态明确。
4. **暗色模式**（可选项，见"需要你确认"）。

**涉及文件**：`styles/admin.css`（重写为按区块组织）、`tokens.css`、`AdminLayout.vue` 微调；每个 admin 视图的 scoped 样式对齐。

## P2 大分类（Category）

**位置**：前台搜索框下方、文件夹网格上方（就是现在 folder-tabs 的位置，将其升级）。

**数据设计（需确认，二选一）**：
- **方案 A（推荐）**：新建 `Category` 表（id/name/icon/sortOrder），`Folder` 加 `categoryId` 可空外键。前台 tabs 显示分类，点击过滤/滚动到该分类下的文件夹；未分类的归入"默认"。后台新增"分类管理"页（增删改、排序、把文件夹拖入分类）。
- **方案 B（零迁移）**：直接把现有 `folder.parentId` 的顶层文件夹当作大分类用，只做前台展示强化 + 后台把"顶层文件夹"单独管理。省一张表，但语义混杂，后续难扩展。

**工作项（按方案 A）**：Prisma migration + repository + `/api/admin/categories` CRUD + navigation payload 带分类 → 前台 tabs 改为分类驱动（保留 scrollspy/药丸滑块）→ 后台 CategoriesView（复用 FoldersView 的排序/表格模式）→ 文件夹编辑表单加"所属分类"下拉。

## P3 前台高度自定义 + 主题预设

现有基础：外观设置已支持卡片/搜索框/弹窗/标签栏/后台表面的圆角、透明度、模糊（`AppearanceEditor`），存 `site.settings`。

**扩展**：
1. **主题预设**：内置 4~6 套完整主题（如：暗夜玻璃 / 极简白 / 蓝紫霓虹 / 纸质暖色 / 高对比无障碍），一键应用 = 一组 accent 色 + 背景方案 + 外观参数 + 字体色的打包。
2. **自定义项扩展**：主色（accent）取色器、背景（纯色/渐变/图片 + 遮罩浓度）、字体（系统字库选择 + 字号档位）、卡片列宽/间距、动画开关（全局关闭动效）。
3. **实现**：`site.settings.theme` 存主题 id + overrides；前台所有颜色改为消费 CSS 变量（P1 已铺路）；AppearanceEditor 上方加主题选择卡片墙（带缩略预览）。

## P4 书签/文件夹管理体验

**痛点推测**（可再补充）：添加书签要填完整表单、不能快速批量加、文件夹和书签分两个页面来回跳。

**改造项**：
1. **快速添加**：书签页顶部一个"粘贴 URL 即添加"输入框——粘贴 URL 自动抓标题/图标（走服务端抓取 meta），选文件夹回车即存。
2. **书签编辑改为侧滑抽屉**（当前是何种形式核实后统一），列表内联编辑名称。
3. **文件夹页**：树形展示（父子层级可视化）、拖拽改排序和层级；文件夹卡片直接显示书签数、"进入该文件夹的书签列表"快捷入口。
4. **书签列表**：文件夹筛选保留，增加"全部"视图 + 按域名分组视图；批量选择工具条常驻优化。
5. **导入流程**：导入预览时可直接映射到分类/文件夹。

## P5 细节与动画

- 统一动效 token：一套 duration（120/240/340ms）+ 两条缓动曲线（standard/spring），全站替换零散的 cubic-bezier。
- 页面切换过渡（router transition，淡入 + 轻微上移）。
- 列表增删动画（`TransitionGroup`）：书签/文件夹增删有位移过渡。
- 弹窗开合改 spring 曲线；toast 出入场统一。
- 骨架屏替代部分 loading 转圈（前台已有 loading bar，后台表格加 skeleton row）。
- 所有新动画纳入 `prefers-reduced-motion`。

---

## 需要你确认的决策

1. **P2 数据设计选 A（新建 Category 表，推荐）还是 B（复用顶层文件夹）？**
2. **后台要不要做暗色模式**（工作量 +30% 左右），还是先把浅色做好？
3. **P4 的痛点**我是按推测写的——你实际操作里最烦的是哪几步？（比如"加一个书签要点几次"）
4. 主题预设的风格方向有没有偏好？我先按"暗夜玻璃/极简白/霓虹/暖纸/高对比"五套出。
